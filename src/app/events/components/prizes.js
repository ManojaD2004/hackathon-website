import React from "react";
import StickyHeader from "./Stickyheader";

const Prizes = () => {
  return (
    <div
      id="prizes-section"
      className=" min-h-screen w-full py-5 px-2 pt-[100px]"
    >
      <StickyHeader title="Prizes" sectionId="prizes-section" />

    
    </div>
  );
};

export default Prizes;